node_modules/
.env
.env.local
out/
*.log
.vercel
.DS_Store

# ── 공개 레포에 올라가면 안 되는 것 ──────────────────────
# 이 레포는 이미지 호스팅 때문에 Public 이다. 아래 둘은 판매자 자산이라
# 올라가면 누구나 내려받고 읽을 수 있다.
# ⚠ 주석은 반드시 줄 맨 앞에. git 은 줄 끝 주석을 무시하지 않고
#    패턴의 일부로 읽어서, 아무것도 제외되지 않는다.

# 판매용 ZIP — 제품 그 자체
dist/
# 패키징 스크립트
pack.mjs
# 판매자용 문서 (가격 전략·마진 메모)
/README.md
