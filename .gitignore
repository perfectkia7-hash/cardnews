node_modules/
.env
.env.local
out/
*.log
.vercel
.DS_Store

# ── 공개 레포에 올라가면 안 되는 것 ──────────────────────
# 이 레포는 이미지 호스팅 때문에 Public 이다. 아래 둘은 판매자 자산이라
# 올라가면 누구나 내려받고 읽을 수 있다.
dist/            # 판매용 ZIP — 제품 그 자체
pack.mjs         # 패키징 스크립트
/README.md       # 판매자용 문서 (가격 전략·마진 메모)
